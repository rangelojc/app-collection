export enum DateFormats {
  standard = "yyyy-MM-DD",
  standard12Hour = "yyyy-MM-DD hh:mm A",
  standardFull = "yyyy MMMM Do, hh:mm A",
}

export enum RouteDefinition {
  LOGIN = "/login",
  INDEX = "/",
  USERS = "/users",
  ORDERS = "/orders",
  ORDER_RESULT = "/orders/result",
}
